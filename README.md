# A Svelte app to calculate simplified stopping criteria for machine-learning prioritised screening in systematic reviews

To deploy the app:

```
# Install dependencies
npm install

# Run app locally 
npm run dev
```
